#include "header.h"

/**
 * _unsetenv - djdcpmscodifopimmd
 * @name: joapdjvkomfoijdsofipjd
 * Return: jjjlfjgaldj
 */
int _unsetenv(const char *name)
{
	char *env;

	env = (_getrealenv(name));
	env = NULL;

	if (!env)
		return (0);

	return (1);
}
